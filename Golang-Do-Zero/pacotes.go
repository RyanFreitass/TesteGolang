package main

import (
	"fmt"
	"strings"
)

func mainPacotes() {
	const name, age = "Ryan", 22
	fmt.Println(name, "is", age, ".")

	fmt.Println(strings.Split("steph", ""))
}

// Tipos:
// bool (TRUE / FALSE)
